# 시험문제 출제 자동화 (CLAUDE.md)

## 프로젝트 개요

이 문서는 Claude Code(클라우드 실행)가 국어 교과 시험문제 출제 작업을 보조하기 위한 지침이다.
Google Drive에 저장된 자료만을 근거로 (1) 문제 유형 분석 보고서를 작성하고, (2) 사용자 승인 후 추천 시험 문제를 작성한다.

1단계~3단계를 한 번 끝까지 진행하는 것을 "사이클"이라 부른다. 아래 지침의 질문 시점·재사용 규칙은 모두 "사이클" 단위로 적용된다.

**핵심 요구사항: 사용량(토큰/LLM 호출) 최적화.** 아래 원칙을 모든 단계에서 우선 적용한다.

---

## 필수 설정값 (사용 전 반드시 아래 값을 채워 넣을 것)

아래 값은 매 사이클 시작 전에 사용자가 직접 입력·확정한다. 값이 비어 있으면 Claude Code는 작업을 시작하지 않고 먼저 질문한다.

| 설정 항목 | 값 (사용자 입력) |
|---|---|
| Drive 루트 폴더 경로/이름 | `{예: /시험문제출제}` |
| Drive 루트 폴더 ID (가능하면 반드시 입력) | `{Drive 폴더 열었을 때 주소창의 .../folders/ 뒤 문자열}` |
| 과목 | `{예: 국어}` |
| 학년 | `{예: 중학교 1학년}` |
| 학기 | `{예: 2학기}` |
| 시험 구분 | `{예: 중간고사 / 기말고사 / 수행평가}` |
| 이번 사이클의 출제 범위 (대단원명) | `{예: 3단원, 4단원}` |

- **루트 폴더 ID를 입력하면** Claude Code는 이름으로 폴더를 검색하지 않고 ID로 바로 접근한다. 이름 검색은 동명 폴더가 여러 개 있을 경우 잘못된 폴더에 접근할 위험이 있고 검색 자체도 호출을 소모하므로, ID가 있으면 항상 ID를 우선한다.
- 처음이라 ID를 모를 경우: 1회차 사이클에서는 이름으로 검색해 진행하되, 폴더를 찾은 직후 그 **폴더 ID를 사용자에게 알려주고** 이 표의 "Drive 루트 폴더 ID" 칸에 채워 넣도록 안내한다. 이후 사이클부터는 반드시 ID로 접근한다.
- 위 표는 CLAUDE.md 상단에 두고, **사이클마다 최신 값으로 갱신**한다(이전 사이클 값이 남아있으면 안 됨). 단, 루트 폴더 ID는 폴더 구조 자체가 바뀌지 않는 한 사이클이 바뀌어도 유지한다.
- 폴더명(01~05번 폴더, 대단원/소단원명 등)은 위 "Drive 루트 폴더 경로/이름"을 기준으로 하되, 사용자가 기존에 다른 폴더명을 쓰고 있다면 그 이름을 그대로 이 표에 반영해 사용한다.

---

## 0. 사용 도구 및 폴더 구조

- **Google Drive 단일 연동**만 사용한다. 다른 도구(Gmail 등)는 사용하지 않는다.
- 최초 실행 시, "필수 설정값"의 **Drive 루트 폴더 경로/이름** 아래에 다음 폴더가 있는지 확인하고 없으면 생성한다.

```
{루트 폴더}/
  ├── 01_시험지양식/
  ├── 02_난이도위계체크리스트/
  ├── 03_출제자료/
  │     ├── {대단원명}/
  │     │     ├── {소단원명}/        # 핵심개념 설명 요약집 + 관련 기출문제 (사용자가 미리 구성해둠, 별도 검증 안 함)
  │     │     └── ...
  │     └── ...
  ├── 04_유형분석보고서/
  │     ├── _소단원별요약/           # 대용량 분할 처리 시 중간 산출물 저장용
  │     └── (대단원 통합 보고서)
  └── 05_추천시험문제/
```

- `03_출제자료`는 반드시 대단원 → 소단원 2단계 구조이며, 각 소단원 폴더에는 그 범위의 핵심 개념 설명 요약집과 관련 기출문제가 들어있다고 전제한다. Claude Code는 이 구성의 완전성을 별도로 검증하지 않는다.
- 폴더가 이미 존재하면 재사용하고, 이름이 다르면 사용자에게 매핑을 1회만 확인한다.

---

## 산출물 형식 원칙

- **사용자에게 보고하는 산출물(유형분석보고서, 추천 시험 문제)은 항상 Word(.docx) 파일로 작성해 Drive에 저장한다.** 사용자가 내용을 직접 수정하거나 복사·붙여넣기 할 수 있도록 하기 위함이다.
- 내부 캐싱용 중간 산출물(위계정의 문서, 소단원별 요약 등 사용자에게 직접 보고하지 않는 파일)은 기존대로 마크다운(.md)으로 저장해도 무방하다. 사용자에게 실제로 공유·보고되는 파일만 .docx로 만든다.

---

## 사용량 최적화 원칙 (모든 단계 공통)

1. **폴더 스캔 최소화**: 루트 폴더 ID가 설정값에 있으면 이름 검색 없이 ID로 바로 접근한다. 그 외 하위 폴더/파일 목록 조회는 각 단계 시작 시 1회만 수행하고 세션 내에서 재사용한다.
2. **소단원 단위로 묶어서 읽기**: 소단원 폴더의 자료(요약집+기출문제)는 한 번에 모아 읽고 한 번에 분석한다. 파일 하나씩 열람·분석하지 않는다.
3. **중간 산출물 캐싱**: 2단계 분석 결과(발문 유형, 위계 체크리스트)는 파일로 저장해두고, 3단계에서는 이 파일만 다시 열람한다. 원본 출제자료를 재분석하지 않는다(단, 지문 전문이 필요할 때는 해당 원본 파일만 개별적으로 열어 전문을 그대로 추출한다 — 이는 "재분석"이 아니라 "단순 복사"이므로 별도).
4. **승인 게이트에서 재작업 최소화**: 사용자가 보고서 일부만 수정 요청하면 해당 부분만 수정하고 전체를 재생성하지 않는다.
5. **질문은 한 번에 모아서, 사이클당 필요한 시점에만**:
   - 난이도 위계(개수·명칭·설명)는 **사이클 시작 시(1단계) 1회만** 질문한다. 이후 2·3단계 진행 중에는 재질문하지 않고, 해당 사이클의 3단계(추천 시험 문제 작성)까지 완료되어야 다음 사이클에서 다시 질문한다.
   - 난이도 상/중/하 비율과 단원별 문항 수는 3단계 시작 시 한 번에 질문한다.
6. **불필요한 서브에이전트/도구 호출 금지**: 여러 서브에이전트를 띄우기보다 하나의 흐름 안에서 순차 처리하고, 유사 구조의 반복 작업(여러 소단원 분석 등)은 프롬프트 구조를 재사용해 길이를 줄인다.

---

## 워크플로우

### 1단계 — 자료 준비 및 난이도 위계 확인

1. "필수 설정값" 표(과목/학년/학기/시험 구분/출제 범위 대단원)가 비어 있으면 먼저 사용자에게 확인해 채운다.
2. 사용자는 `01_시험지양식`에 시험지 양식을, `03_출제자료/{대단원}/{소단원}/`에 그 범위의 핵심 개념 요약집과 관련 기출문제를 미리 넣어둔다.
3. 새 사이클이 시작될 때, Claude Code는 다음을 **한 번에** 질문한다:
   - 난이도 위계의 **개수**
   - 각 위계의 **명칭**
   - 각 위계의 **설명(정의 기준)**
4. 답변을 받아 `02_난이도위계체크리스트/위계정의.md`로 저장한다(해당 사이클의 3단계까지는 재질문하지 않고 이 파일을 참조).

### 2단계 — 출제자료 분석 및 유형분석보고서 작성 (승인 게이트)

1. **분석 항목** (매력적 오답 파악은 컨텍스트 부담으로 제외):
   - **발문 유형 분류** (자료에서 실제 관찰되는 발문 유형)
   - **문제 유형 간 흐름** (문항 배치 순서, 난이도 상승 패턴 등)
   - **난이도 위계 기반 체크리스트** (1단계에서 저장한 위계 정의를 기준으로 자료 문항을 위계별 분류)
2. **대용량 소단원 분할 처리 fallback** (한 번에 읽기에 자료가 너무 크거나 파일 수가 많은 경우):
   - 소단원 폴더 내 파일을 개별적으로 순차 분석하되, 원문 전체를 계속 들고 있지 않고 파일별로 "발문 유형 태그 + 위계 태그" 형태의 **짧은 요약만** 생성해 `04_유형분석보고서/_소단원별요약/{소단원명}.md`에 저장한다.
   - 해당 소단원의 모든 파일 요약이 모이면, **원문을 다시 열지 않고 요약 파일들만** 모아 소단원 단위 통합 분석을 수행한다.
   - 대단원 수준 보고서는 소단원별 통합 분석 결과만 모아 작성하고, 원본 파일을 재열람하지 않는다.
   - 이 분할 처리는 2단계(분석)에만 적용되며, 3단계에서 지문 전문이 필요할 때는 해당 원본 파일만 그때 개별적으로 열어 전문을 추출한다.
3. 위 내용을 종합해 **유형분석보고서**를 작성하고 `04_유형분석보고서/{대단원명}_유형분석보고서.docx`로 저장한다(산출물 형식 원칙에 따라 Word 파일로 작성).
4. **승인 게이트**: 보고서의 개요와 핵심 내용을 사용자에게 요약 보고하고, 명시적 승인을 받아야 3단계로 진행한다.
   - 사용자가 부분 피드백을 주면 해당 부분만 수정해 재보고한다(수정본도 동일하게 .docx로 갱신).
   - **부분 수정을 반영한 뒤에도 "승인함"과 같은 명시적 승인 문구가 없으면 절대 3단계로 진행하지 않는다.** (수정 = 승인 아님. 반드시 별도의 승인 의사표현이 있어야 함)

### 3단계 — 추천 시험 문제 작성

1. 3단계 시작 시, 사용자에게 **한 번에** 다음을 질문한다:
   - 난이도 **상/중/하 비율**
   - **단원별 문항 수**
2. **지문 처리 원칙 (국어 과목 특성)**: 지문은 해당 소단원 출제자료에 주어진 **작품 전문을 그대로 사용**한다. 지문을 새로 창작하거나 요약하지 않는다.
3. **발문 처리 원칙**: 발문은 원자료의 발문을 그대로 베끼지 않고, 2단계에서 정리한 **발문 유형만 참고**하여 새로운 발문으로 작성한다.
4. `01_시험지양식`의 서식과 `04_유형분석보고서`의 승인된 분석 내용(발문 유형, 흐름, 위계 체크리스트)을 근거로 추천 문제를 작성한다.
5. 결과물을 시험지 양식에 맞춰 `05_추천시험문제/`에 **Word(.docx) 파일**로 저장하고(산출물 형식 원칙에 따름), 사용자에게 단원별·난이도별 구성 요약과 함께 보고한다.
6. **최종 검증은 수행하지 않는다.** 정답·배점·형식 등의 최종 오류 검증은 사용자가 직접 수행하며, Claude Code는 컨텍스트 절약을 위해 별도의 자동 검증 단계를 두지 않는다.
7. 3단계 완료·보고를 끝으로 해당 사이클이 종료된다. 다음 사이클 시작 시 "필수 설정값" 표를 새 사이클 값으로 갱신하고, 1단계의 위계 질문부터 다시 진행한다.

---

## 산출물 요약

| 단계 | 산출물 | 저장 위치 |
|---|---|---|
| 1 | 위계 정의 문서 (내부용, .md) | `02_난이도위계체크리스트/위계정의.md` |
| 2 | (분할 처리 시) 소단원별 요약 (내부용, .md) | `04_유형분석보고서/_소단원별요약/` |
| 2 | 유형분석보고서 (승인 필요, **.docx**) | `04_유형분석보고서/` |
| 3 | 추천 시험 문제 (양식 적용, 지문 전문 그대로, **.docx**) | `05_추천시험문제/` |
